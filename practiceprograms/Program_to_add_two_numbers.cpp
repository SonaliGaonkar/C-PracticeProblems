#include <iostream>

int main(){
    int x,y,sum;
    std::cout << "Enter two numbers : ";
    std::cin >> x >> y;
    sum = x + y; 
    std::cout << "sum of two numbers is : " << sum;

    return 0;
}